// Update this URL to point to your Prismic URL
// It will look something like: https://mysite.cdn.prismic.io/api/v2

const PRISMIC_API_URL = '<YOUR_URL_HERE>'

module.exports = {
    PRISMIC_API_URL,
}
